type Transaction {
  _id: String!
  sender: Account!
  recipient: Account!
  amount: Int!
  timestamp: String!
}

input AirdropInput {
  sender: String!
  recipient: String!
  amount: Int!
  reason: String
}

extend type Mutation {
  airdropPoints(args: AirdropInput!): String
}
